# 为《气闸孤注：γ-7》贡献

感谢你愿意帮助改进游戏。为了让改动容易审查和回归，请遵循下面的约定。

## 开始之前

1. 先搜索现有 Issue，确认问题或提案尚未被讨论。
2. 功能改动请先说明玩家体验、规则影响和联机状态变化。
3. 不要提交密钥、真实玩家资料、受版权保护但无授权的素材，或未经本人同意的角色模型。

## 本地验证

使用当前 Node.js LTS 版本安装依赖并运行测试：

```bash
npm ci
npm run test:all
```

如果改动涉及 3D、菜单或联机流程，还请在浏览器中手动验证 `deepspace.html`；
低配界面改动同时验证 `deepspace-flat.html`。

## 提交 Pull Request

- 一次 PR 聚焦一个问题，说明改了什么、为什么改、如何验证。
- 新规则或道具必须同时补充规则测试，并确认不会向客户端泄露隐藏弹序。
- 用户输入进入 HTML 前必须保持文本转义或使用 `textContent`。
- 新的外部代码或素材必须注明来源、版本与许可证。
- 不要提交 `assets/models/*.glb`；模型需由使用者在本地自行提供并确认权利。

## 历史项目边界

`index.html`、`css/style.css`、`js/editor.js`、`js/game.js`、`js/i18n.js`、`js/interp/`、
`js/levels.js` 和 `tests/` 属于保留但不维护的「侠盗猎码」历史项目。
除非 Issue 明确要求，请不要修改这些路径。

## 行为准则

请围绕事实和代码友善讨论，尊重不同经验水平，并避免骚扰、歧视或泄露他人隐私。
